<section>
    <h2>Edit Transaction</h2>

    <form method="POST">
        {{#with transaction}}
        <label for="transaction-date">Date:</label>
        <input type="date" id="transaction-date" name="date" value="{{../transaction.date}}">

        <label for="transaction-source">Sender:</label>    
        <input type="text" id="transaction-source" name="transactionSource" required value="{{transactionSource}}">
        <div id="segment-group">
           
            {{#each segments}}
                <div data-segment='main'>
                    <label for="segment-amount">Amount:</label>    
                    <input type="number" id="segment-amount" name="amount" required value={{amount}}>
                    
                    <label for="segment-category">Category</label>    
                    <select id="segment-category" name="categoryId" required>
                        {{#each ../../categories}}
                            <option {{#compare name "===" ../categoryId.name}}selected{{/compare}} value="{{_id}}" >{{name}}</option>
                        {{/each}}
                    </select>
                </div>
              
            {{/each}}

        </div>
        
          <div>
            <button type="button" id="split-button">Split transaction</button>
         </div>
        <label for="transaction-notes">Notes</label>
        <textarea id="transaction-notes" name="notes">{{notes}}</textarea>
        {{/with}}
        <button type="sumbit">Update</button>
        
    </form>
</section>

<script src="/scripts/add-segment.js"></script>